"""Command-line entry points.

``navable-collect list`` runs on vanilla Python (no Isaac boot needed) —
everything else spins up a headless ``SimulationApp`` via
:mod:`navable_synth_data_collector.cli.bootstrap`.
"""
